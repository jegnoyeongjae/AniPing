// Gnb.jsx는 Header.jsx에 통합되었으므로 더 이상 사용되지 않거나,
// Header 내부에서 사용되는 경우 수정이 필요할 수 있습니다.
// 현재 요청에서는 Header.jsx가 Gnb 기능을 포함하도록 변경되었으므로
// 이 파일은 비워두거나 삭제할 수 있지만, 안전을 위해 내용은 유지하되 사용하지 않도록 합니다.

const Gnb = () => {
  return null;
};

export default Gnb;
